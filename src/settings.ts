export default interface WingmanSettings {
    sectionNoteCurrentEnabled: boolean;
    sectionNotesSimilarEnabled: boolean;
}


export const DEFAULT_SETTINGS: WingmanSettings = {
    sectionNoteCurrentEnabled: true,
    sectionNotesSimilarEnabled: true,
}
